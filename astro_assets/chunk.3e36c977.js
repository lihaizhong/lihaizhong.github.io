const id = "JavaScript中的const、let和var的区别.mdx";
						const collection = "blog";
						const slug = "javascript中的constlet和var的区别";
						const body = "\nJavaScript是一门弱类型语言。在ES3和ES5时期，我们都是通过`var`来声明一个变量的。那个时候的前端开发相对简单，JavaScript通常是被用来做些景上添花的小功能，所以完全能够应付我们的日常开发工作。但是随着移动互联网的兴起，H5应用适应快速迭代的属性使前端得到了快速发展，同时也凸显出了JavaScript在大型项目开发中的劣势。因此，语言的升级势在必行，而变量声明的方式则是其中一个需要优化的内容。所以，今天我们的主题就是说说变量声明————`const`和`let`。\n\n## 变量的创建\n\n首先，我要讲清楚变量声明存在三个步骤：\n1. 创建变量；\n2. 初始化变量；\n3. 给变量赋值。\n\n这三个步骤的微妙差异，正是`const`、`let`、 `var`之间的不同之处。那下面我们来好好讲讲他们之间的差异。\n\n## const和let\n\n**`const`命令是指声明一个常量。`const`只能进行变量的创建和变量初始化，而不能进行变量赋值。** 也就是说，`const`只能进行上面说的1、2步。\n\n```javascript\nconst a = 1\na = 2 // Uncaught TypeError: Assignment to constant variable.\n```\n\n可以看到，当我们对`const`声明的变量进行赋值时，浏览器报了相应的错误。\n\n**`let`命令是指声明一个变量。`let`可以进行变量的创建、变量的初始化和变量的赋值。**\n\n```javascript\nlet a = 1\na = 2\nconsole.log(a) // 2\n```\n\n可以看到，当我们对`let`声明的变量进行赋值时，变量对应的值会进行相应的改变。\n\n## const、let与var的区别\n\nJavaScript是一门弱类型语言。它不会在声明时要求变量确定类型，甚至你可以随时修改变量的类型。那既然是弱类型语言，`var`似乎足以满足所有的需求了，为什么还需要`const`和`let`呢？\n\n**原因是`var`太灵活了。** 很多时候，我们无法对`var`进行约束，这会导致我们的程序中出现意想不到的问题。而`const`和`let`就是用来约束我们变量的使用，尽可能地减少意想不到的问题。\n\n1. `var`存在变量提升的问题。\n2. `var`不支持块级作用域。\n3. `var`可以在静默状态下重复声明。\n\n上面我们已经讲到了`var`的问题，那么我就从这三个问题出发来讲讲`const`、`let`、`var`三者之间的区别。\n\n### 作用域\n\n作用域在高级语言中是一个非常重要的概念。作用域相当于一个容器，变量和函数都放在这个容器里面。理论上来说，容器外面不能访问容器里面声明的变量和函数，但容器内可以调用父祖级容器的变量和函数。举个栗子：\n\n``` javascript\nvar a = 1\n\nfunction foo() {\n  var b = 2\n  console.log(a) // 1\n}\n\nfoo()\nconsole.log(b) // Uncaught ReferenceError: b is not defined\n```\n\n这里`function foo`创建了一个**foo scope**，全局有一个**global scope**。\n\n*`global scope`中存在变量a和函数foo，`foo scope`中存在变量b。在`foo scope`中，我们能访问到变量a；但在`global scope`中，我们访问不到变量b，这就是作用域。而`global scope`与`foo scope`之间的关系，我们称之为作用域链。*\n\nES3和ES5中，**作用域的创建方式**只有**function**、**with**和**catch**。`function`是常用的创建作用域的方式；`with`已经被抛弃，因为它会导致作用域链发生变化，导致浏览器所有的优化都失效；`catch`经常用于捕获error。\n\n**ES6有了块级作用域的概念。** **const**和**let**命令就是作用于块级作用域。\n\n```javascript\n{\n  var a = 1\n  let b = 2\n  const c = 3\n}\nconsole.log(a) // 1\nconsole.log(b) // Uncaught ReferenceError: b is not defined\nconsole.log(c) // Uncaught ReferenceError: c is not defined\n```\n\n从上面的例子中可以看到，`var`命令不受块级作用域的影响，可以正常访问；`const`和`let`受到块级作用域的影响，在块级作用域以外无法正常访问。依据这个特性，我想理解`for`循环的这个问题应该会变得容易很多：\n\n```javascript\nfor (var i = 0; i < 6; i++) {\n  setTimeout(function() {\n    console.log(i) // 6, 6, 6, 6, 6, 6\n  }, 20)\n}\n\nfor (let i = 0; i < 6; i++) {\n  setTimeout(function() {\n    console.log(i) // 0, 1, 2, 3, 4, 5\n  }, 20)\n}\n```\n\n这道题非常的经典。从*作用域*的角度能很好的解释问题的原因。\n\n### 变量提升\n\n谈到JavaScript，我们知道`var`和`function`会存在变量提升的情况，也就是即使调用在声明之前，变量和方法依然能够访问而不会报错。\n\n```javascript\nconsole.log(a) // undefined\nfoo() // foo\n\nvar a = 1\n\nfunction foo() {\n  console.log('foo')\n}\n```\n\n上面的例子我们发现，即使`var`定义的变量写在声明之前依然能够访问，只是返回的值为undefined；`function`也是能正常访问，还能得到预期的结果，这就是变量提升。\n\n在JavaScript中，一个变量首先会被**创建**，然后是**初始化**，最后就是**赋值**。\n\n*我们从上面的代码来看这三个操作，首先JavaScript引擎会找到`var a`，然后告诉作用域创建一个名称为a的变量；然后会马上初始化它，此时初始化的值为undefined；最后代码执行到编写的位置时会给变量a赋值为1。也就是说，我们`console.log(a)`的结果为undefined的原因是变量a的创建和初始化的过程被提升了。*\n\n```javascript\nconsole.log(a) // 1\nconsole.log(b) // Uncaught ReferenceError: b is not defined\nconsole.log(c) // Uncaught ReferenceError: c is not defined\n\nvar a = 1\nlet b = 2\nconst c = 3\n```\n\n这里我们试验一下`const`和`let`命令，发现他们都报错了。那么是不是说`const`和`let`不存在变量提升呢？\n\n其实严格意义上来讲，`const`和`let`依然存在变量提升的情况。只是与`var`不同的是，`const`和`let`只是创建被提升了。我们来看看ES6标准的原话\n\n*The variables are created when their containing Lexical Environment is instantiated but may not be accessed inany way until the variable’s LexicalBinding is evaluated.*\n\n简单来说就是当环境作用域被创建时，变量就都已经被创建了。只是这个时候我们还未进行声明，在这个是时间点访问会导致直接报错。而从创建变量到声明变量这个时间段，我们就称它为**临时性死区（Temporal Dead Zone）**\n\n### 重复声明\n\n```javascript\nvar a = 1\nvar a = 2\nlet a = 3 // Uncaught SyntaxError: Identifier 'a' has already been declared\nlet b = 1\nlet b = 2\nvar b = 3 // Uncaught SyntaxError: Identifier 'b' has already been declared\nconst a = 1 // Uncaught SyntaxError: Identifier 'a' has already been declared\nconst b = 3 // Uncaught SyntaxError: Identifier 'b' has already been declared\nconst c = 1\nconst c = 2 // Uncaught SyntaxError: Identifier 'c' has already been declared\nvar c = 3 // Uncaught SyntaxError: Identifier 'c' has already been declared\nlet c = 4 // Uncaught SyntaxError: Identifier 'c' has already been declared\n```\n\n上面的例子，我们进行了很多重复声明。结果发现，\n\n1. `var`命令和`let`命令可以进行重复声明，`const`命令不能进行重复声明。\n2. 重复声明时，命令必须与之前的相同，否则会报错。\n\n## 总结\n\n1. `var`没有块级作用域概念，`const`和`let`有块级作用域概念。\n2. `var`会提升创建和初始化，`const`和`let`只会提升创建。\n3. `const`不可以重复声明，`let`和`var`可以重复声明。\n";
						const data = {title:"JavaScript中的const、let和var的区别",description:"",pubDate:new Date(1591020180000),heroImage:""};
						const _internal = {
							type: 'content',
							filePath: "/Users/lihaizhong/Documents/Project/lihaizhong.blog/src/content/blog/JavaScript中的const、let和var的区别.mdx",
							rawData: "\ntitle: \"JavaScript中的const、let和var的区别\"\ndescription: \"\"\npubDate: Mon Jun 01 2020 22:03:00 GMT+0800\nheroImage: \"\"",
						};

export { _internal, body, collection, data, id, slug };
